<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.courses.bogdan.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service android:name=".services.MyService"></service>
        <receiver android:name=".activities.MainActivity$MyReceiver">
            <intent-filter>
                <action android:name="com.courses.EXAMPLE"></action>
                <action android:name="com.courses.EXAMPLEFragment"></action>
            </intent-filter>

        </receiver>

    </application>

</manifest>
